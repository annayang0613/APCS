#include <bits/stdc++.h>
using namespace std;

int main(){
  int row, column;
  int a[100][100];
  while(cin >> row >> column) {
    for(int i=0; i<row; i++) { //輸入原本陣列
      for(int j=0; j<column; j++) {
        cin >> a[i][j];
      }
    }
    for(int i=0; i<column; i++) { //翻轉陣列
      for(int j=0; j<row; j++) {
        cout << a[j][i] << " ";
      }
      cout << endl;
    }
  }
  return 0;
}
