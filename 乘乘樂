#include <bits/stdc++.h>
using namespace std;

int main(){
  int t;
  string a;
  cin >> t ;
  int ans=1;
  for(int j=0; j<t; j++){
    cin >> a;
    for(int i=0; i<a.length(); i++){
      ans *= ( a[i]-'0' ); //把ASCII轉字元
    }
    cout << ans << endl;
    ans=1;
  }
  return 0;
}
